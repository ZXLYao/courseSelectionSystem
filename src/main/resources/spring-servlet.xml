<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.2.xsd">

    <!--配置扫描的controller，扫描base-packge下面的类包及其子包下的类-->
    <context:component-scan base-package="com.yao.controller"/>
    <!-- 静态资源访问，例如图片、js文件、css文件等，访问失败，跳转静态页面，否则出现404 -->
    <mvc:default-servlet-handler/>
    <!-- 注解驱动。开启注解，自动注册RequestMappingHandleMapping和RequestMappingHandlerAdapter两个bean，并提供数据绑定支持 -->
    <mvc:annotation-driven/>
    <!-- jspViewResolver jsp视图解析器 -->
    <bean id="jspViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="prefix" value="/WEB-INF/jsp"/><!-- 后台默认返回视图寻找视图文件的路径 -->
        <property name="suffix" value=".jsp"/><!-- 后台返回视图默认添加的后缀 -->
    </bean>



</beans>